# ACV2 - physics-informed refrigerant-leak localisation
# Everything here is already required by the wider nebulax_p3 backend.
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
openpyxl>=3.1
pyarrow>=12.0      # Parquet cache for the 34 MB rich-schema workbook
joblib>=1.3        # weight artefact only - there is no pickled estimator
